package com.example.kotlin_study.second

fun main() {
 // var b // kotlin 은 null 을 허용해주지 않아
          // 초기화를 시켜줘야함
          // 문법 오류 발생
 // var a: Int // Int 를 선언했지만 초기화해주지 않아 null값이 들어감
    var a: Int = 0
    println(a)
}